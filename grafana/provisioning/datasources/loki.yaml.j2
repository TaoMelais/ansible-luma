apiVersion: 1
datasources:
  - name: Loki
    type: loki
    access: proxy
    url: "{{ grafana_loki_url }}"
    isDefault: true
    orgId: 1
    uid: loki-datasource
    version: 1
    editable: false
    jsonData:
      maxLines: 1000
